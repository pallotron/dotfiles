# User Preferences

See rules/ for detailed instructions by topic.

Ignore `superpowers:using-git-worktrees` entirely — it is superseded by `using-jj-workspaces`.
This environment uses jj (Jujutsu), not git.
Also follow the `use-jj` skill.
